import { StyleSheet, Text, TextInput, TextInput } from 'react-native'
import React from 'react'

const Input = () => {
  return ( <TextInput>Input</TextInput>
  )
}

export default Input

const styles = StyleSheet.create({})